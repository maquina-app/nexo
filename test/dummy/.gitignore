# Transient dummy-app artifacts — regenerated by the Rails-path verify
# (bin/rails g nexo:workflows && bin/rails db:migrate).
/db/migrate/*_create_nexo_workflow_runs.rb
/db/schema.rb
/storage/*.sqlite3
/log/*.log
/tmp/
